<wxs src="WXS/base.wxs"
    module="Utils" />
    
<view class="oak-label oak-label-{{ size }} iconfont {{ icon ? iconPrefix + '-' + icon : '' }} {{ checked ? 'oak-label-checked' : '' }} ext-class" style="{{ styles }}">
    <view class="oak-label-content">
        <slot></slot>
    </view>
</view>

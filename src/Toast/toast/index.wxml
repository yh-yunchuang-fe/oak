<wxs src="WXS/base.wxs" module="Utils" />

<view animation="{{animation}}" class="oak-toast-title" style="max-width: {{maxWidth}}">
    <oak-icon wx:if="{{icon}}" name="{{icon}}" />
    {{title}}
</view>